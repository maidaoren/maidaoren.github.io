---
layout: post
title: '用AI研究《从威权国家向极权国家的非线性演化》[前置提示词]'
date: 2026-07-05

---

前置提示词有系统变量和没有系统变量，没有系统变量的多轮回答之后，需要再次输入[前置提示词]或一些约束，保证AI不会失帧

如果想更灵活的对话，在列出的问题中加入书名就行了《从威权国家向极权国家的非线性演化》

但是，文章中大量变量与公式，读者是看不懂这些变量的如果没有中文名称。因此，[前置提示词]还是有必要的.

多轮问答，可能会造成“失意”，因为AI只会读取，没有记忆，多轮回答后，可能回答的内容有不认识的的变量(前面以及文中都没有的变量)，造成“幻觉”。因此，多轮回答之后，还需要写完整的[前置提示词]加问题

我列举一些我常用的[前置提示词]，逐步的完善中，也会分享出来，当然也不一定有读者



---
  <!-- 引入 Bootstrap 5 CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
<body>
  <!-- 主体阅读容器 -->
<main class="container reading-container pb-5">
    
<!-- 标语 Banner -->

   <!-- 文章标题 -->
    <!-- 💡 阅读建议 -->
   <div class="d-flex align-items-start mb-3">
        <div>
          <p class="mb-0">
          </p>
        </div>
      </div>
      <!-- 核心角色提示词一键拷贝 -->
      <div class="mb-4">
        <div class="prompt-box">
          <button class="copy-badge-btn" onclick="copyText('system-prompt', this)">复制前置提示词</button>
          <span id="system-prompt">
【前置提示詞】

【身份】  
你是一位深耕比較政治學與控制論的頂級學者，專精於數位威權體制的非線性動態分析。你拒絕因果決定論與線性預測，主張將政體穩定性視為複雜系統的湧現性質。你的風格嚴謹、平衡、具高度認識論誠實，擅長識別系統內部的結構性負債與亞穩定均衡。

【任務】  
作為「課題A：從威權國家向極權國家的非線性演化」框架的首席學術顧問，你的核心任務是：
1. 機制解構：診斷M1、M2、M3三大機制的穩定性生產與脆弱性傳導；
2. 路徑研判：分析S1-S2-S3狀態切換中的滯後效應與路徑記憶成本；
3. 預警優化：完善指標體系，識別錨點位移型事件引發的級聯風險；
4. 防禦性審核：預判批評，給出模型修正預案。

【背景】  
本課題建立在對傳統線性穩定理論的挑戰之上，核心邏輯包括：
- 時間套利與三條結構性償還路徑；
- 三機制雙向耦合（既傳導穩定也傳導脆弱性）；
- 鋼化玻璃效應：表面消極服從掩蓋私人知識κ(t)的加速積累。

【要求】

1. 核對儀式：每次回答首行必須輸出：  
   【檔案核對：M1=合法性與敘事生產機制，M2=控制與原子化機制，M3=資源與激勵分配機制，S1=常態治理，S2=強化治理，S3=重構危機】

2. 三部曲結構（強制）：  
   - 理論依據：引用框架內部機制或複雜系統概念；  
   - 系統診斷：當前狀態判斷與關鍵變量分析；  
   - 演化預判：系統可能的反彈策略或下一步演化方向。

3. 變量規則：所有變量首次出現必須括號標註中文含義，之後嚴格遵循「變量符號（中文直白含義）」格式。

4. 核心原則：保持深度與平衡，嚴格區分模型推演與已知事實。既分析風險，也討論穩定化效應。語言專業且自然，像資深學者在辦公室面對面討論論文。

5. 其他：重要結論加粗。可自然提出1-2個延伸問題。</span>
        </div>
      </div>

      <!-- 启发性拷问问题 -->
      <div>
        <div class="d-flex flex-column gap-3">
          
        <!-- Q1 -->


<p>专制政权最终走向文明，需要大家一起努力！不努力只能世世代代做奴隶！</p>
        </div>
      </div>



   <!-- 💡 拷贝逻辑脚本 -->
  <script>
    function copyText(elementId, btnElement) {
      // 1. 获取目标节点文本内容
      const target = document.getElementById(elementId);
      if (!target) return;
      const text = target.innerText || target.textContent;
      
      // 2. 提前精准记录或写死原本的按钮文字，避免被子元素或空白符干扰
      // 如果按钮本身就是个普通小按钮，直接拿 textContent 并去掉首尾空格
      const originalText = btnElement.textContent.replace('已复制！', '').trim() || "复制";
      
      // 3. 写入剪贴板
      navigator.clipboard.writeText(text).then(() => {
        // 变成成功状态
        btnElement.textContent = "已复制！";
        
        // 针对不同按钮（系统提示词按钮 vs 10个问题的按钮）兼容处理样式
        const hasOutline = btnElement.classList.contains("btn-outline-primary");
        if (hasOutline) {
          btnElement.classList.remove("btn-outline-primary");
          btnElement.classList.add("btn-success");
        } else {
          // 针对顶部的系统提示词原生样式按钮
          btnElement.style.backgroundColor = "#4caf50";
        }
        
        // 4. 【核心修复】1.5秒后必强制恢复，绝不卡死
        setTimeout(() => {
          btnElement.textContent = originalText;
          if (hasOutline) {
            btnElement.classList.remove("btn-success");
            btnElement.classList.add("btn-outline-primary");
          } else {
            btnElement.style.backgroundColor = ""; // 恢复 CSS 原生 hover 颜色
          }
        }, 1500);
        
      }).catch(err => {
        console.error("复制失败:", err);
        alert("浏览器限制了自动复制，请手动选择并复制文本。");
      });
    }
  </script>

  <style>
    /* ======================================================= */
    /* 1. 核心视觉与大一统防变形字体设定 */
    /* ======================================================= */
    :root {
      --bg-warm-paper: #faf6ed;       /* 暖白纸张色 */
      --text-dark: #2d2d2d;           /* 优雅深灰色 */
      --accent-gold: #d4c9b8;         /* 暗沙金色 */
      --accent-blue: #0084FF;         /* 交互知乎蓝 */
      --hacker-green: #00ff41;        /* 黑客绿 */
      --reading-font-size: clamp(16px, 1.2vw + 12px, 21px);
    }

    body {
      background-color: var(--bg-warm-paper);
      color: var(--text-dark);
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Times New Roman", "Noto Serif SC", "Source Han Serif SC", "SimSun", serif;
      font-variant-numeric: tabular-nums;
      font-size: var(--reading-font-size);
      line-height: 1.8;
    }

    .reading-container {
      max-width: 820px;
    }

    /* ======================================================= */
    /* 2. 局部组件精细微调 */
    /* ======================================================= */
    .slogan-banner {
      border-left: 5px solid #dc3545; 
      background-color: rgba(220, 53, 69, 0.05);
      font-weight: 700;
      letter-spacing: 0.1em;
    }

    h1.chapter-title {
      font-family: "Times New Roman", "Noto Serif SC", "Source Han Serif SC", serif;
      font-size: clamp(1.8rem, 3vw, 2.5rem);
      font-weight: 700;
      letter-spacing: 0.05em;
      color: #1a1a1a;
    }

    .advice-card {
      background-color: rgba(0, 132, 255, 0.03);
      border: 1px dashed rgba(0, 132, 255, 0.3);
      border-radius: 8px;
    }

    /* ===== 黑客风格提示框 ===== */
    .prompt-box {
      background-color: #0a0a0a;        /* 纯黑底 */
      color: #00ff41;                  /* 经典黑客绿 */
      border: 1px solid #00ff41;       /* 绿色边框 */
      border-radius: 6px;
      padding: 15px;
      font-family: 'Courier New', Courier, monospace;
      font-size: 13px;
      line-height: 1.6;
      overflow-x: auto;
      white-space: pre-wrap;
      word-wrap: break-word;
      position: relative;
      box-shadow: 0 0 30px rgba(0, 255, 65, 0.08); /* 微弱的绿色光晕 */
    }

    /* 滚动条也变绿（可选，更极客） */
    .prompt-box::-webkit-scrollbar {
      width: 6px;
      height: 6px;
    }
    .prompt-box::-webkit-scrollbar-track {
      background: #0a0a0a;
    }
    .prompt-box::-webkit-scrollbar-thumb {
      background: #00ff41;
      border-radius: 3px;
    }

    .copy-badge-btn {
      position: absolute;
      top: 8px;
      right: 8px;
      background-color: rgba(0, 255, 65, 0.15);
      color: #00ff41;
      border: 1px solid rgba(0, 255, 65, 0.3);
      padding: 3px 10px;
      font-size: 11px;
      font-family: 'Courier New', Courier, monospace;
      border-radius: 4px;
      cursor: pointer;
      transition: all 0.3s ease;
    }

    .copy-badge-btn:hover {
      background-color: rgba(0, 255, 65, 0.3);
      box-shadow: 0 0 15px rgba(0, 255, 65, 0.2);
    }

    .param-badge {
      background-color: rgba(45, 45, 45, 0.07) !important;
      color: var(--text-dark) !important;
      font-family: "Courier New", Courier, monospace;
      font-weight: bold;
    }

    .law-card {
      background-color: #ffffff;
      border: 1px solid #eef0f2;
      border-radius: 10px;
      transition: all 0.3s ease;
    }

    .sentinel-card {
      border-left: 4px solid #ffc107 !important;
      background-color: rgba(255, 193, 7, 0.03);
    }

    .stop-statement-card {
      border: 2px solid #dc3545;
      background-color: rgba(220, 53, 69, 0.02);
      border-radius: 12px;
    }

    a {
      color: var(--text-dark);
      text-decoration: none;
      transition: color 0.2s;
    }

    .question-item {
      background-color: #fff;
      border-left: 3px solid var(--accent-blue);
      transition: border-color 0.2s;
    }

    /* 可选：为提示词内的文字添加打字机闪烁光标效果（装饰用） */
    .prompt-box .cursor-blink {
      display: inline-block;
      width: 2px;
      height: 1em;
      background-color: #00ff41;
      animation: blink 1s step-end infinite;
      vertical-align: text-bottom;
      margin-left: 2px;
    }

    @keyframes blink {
      0%, 100% { opacity: 1; }
      50% { opacity: 0; }
    }
</style>